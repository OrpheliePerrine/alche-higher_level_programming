#!/usr/bin/python3
def islower(c):
    '''Check if c is lowercase'''
    if (c) is_lowercase:
        return 'a' <= char <=  'z':
            print("{} is lower".format(c))
    else:
        return 'A' <= char <= 'Z':
            print("{} is upper".format(c))
